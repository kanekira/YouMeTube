import VideoList from "./VideoList";

const Body: React.FC = () => {
    return (
        <main className="mt-6">
            <h2 className="mb-4 text-2xl font-bold">Recommended</h2>
            <VideoList />
        </main>
    );
};

export default Body;
